# Port of the old sonata.yml `sonata_doctrine_orm_admin` section, which only declared the
# (empty) default `templates.types` maps — i.e. stock behavior. Defaults are kept; the section
# exists so the bundle is explicitly configured and future overrides have a home.
sonata_doctrine_orm_admin:
    entity_manager: ~
